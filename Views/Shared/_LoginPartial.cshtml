@using Microsoft.AspNetCore.Identity
@using reffffound.Data

@inject SignInManager<ApplicationUser> SignInManager


<ul>
	@if (SignInManager.IsSignedIn(User))
	{
		<li class="">
			<a class="" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
		</li>
		<li class="">
			<form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Bookmarks", 1)">
				<button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
			</form>
		</li>
		<!-- AT if (ViewBag.ShowUserFunctions)
		{
			<li>Users functions</li>

		 atif (ViewBag.ShowAdminFunctions)
			{
				<li>Admins functions</li>
			}
		}
		-->
	}
	else
	{
		<li class="nav-item">
			<!-- <a class="nav-link text" asp-area="Identity" asp-page="/Account/Register">Register</a> -->
			<a class="nav-link text" asp-controller="Home" asp-action="Contact">Register</a>
		</li>
		<li class="nav-item">
			<a class="nav-link text" asp-area="Identity" asp-page="/Account/Login">Login</a>
		</li>
	}
</ul>
